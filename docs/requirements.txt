sphinx
sphinx-book-theme
breathe
# Knit-branded Pygments styles (knit-light / knit-dark). Editable local install;
# path is relative to the repo root, where `make` runs pip. Replaces the former
# third-party `pygments-styles` (min-light / min-dark).
-e docs/pygments-knit
